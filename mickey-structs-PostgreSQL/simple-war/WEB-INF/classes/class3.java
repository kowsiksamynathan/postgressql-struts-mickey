import com.opensymphony.xwork2.ActionSupport;import java.io.*;import com.adventnet.persistence.*;import com.adventnet.ds.query.*;import com.adventnet.db.api.RelationalAPI;import java.sql.*;import java.util.Iterator;public class class3 extends ActionSupport{        private String udeg;    private String uname;        private String msg;            public String execute() throws Exception {                             String s=getUdeg();              Long s2=Long.parseLong(s);        String s1=getUname();         Criteria c = new Criteria(new Column("Employee", "EMP_ID"),s2, QueryConstants.EQUAL);            DataObject d =DataAccess.get("Employee",c);            Row r = d.getRow("Employee");                                    // set the modified value in the row            r.set("EMP_NAME",s1);                        //update the modified row in DO            d.updateRow(r);                        //Persists the changes done in the given dataObject into the database.            DataAccess.update(d);                // Row r = new Row ("Employee");                // r.set("EMP_ID",s2);                // r.set("EMP_NAME",s1);                // DataObject dataObject = new WritableDataObject();            // dataObject.addRow(r);                // DataAccess.update(dataObject);                    setMsg("successfully updated");                    return "success";                }            public String getUname() {        return uname;    }    public void setUname(String uname) {        this.uname = uname;    }    public String getUdeg() {        return udeg;    }    public void setUdeg(String udeg) {        this.udeg = udeg;    }    public String getMsg() {        return msg;    }    public void setMsg(String msg) {        this.msg = msg;    }                          }